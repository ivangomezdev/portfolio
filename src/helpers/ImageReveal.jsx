import React, { useState, useEffect } from 'react';

const ImageReveal = ({ src, alt }) => {


  return (
    <div id={src} style={{ minHeight: '300px', margin: '20px 0' }}>
 
    </div>
  );
};

export default ImageReveal;